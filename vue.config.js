// const path = require('path')

module.exports = {
  publicPath:'/appeal/',
  outputDir:'dist/appeal/',
  assetsDir:'static',
  lintOnSave: process.env.NODE_ENV === 'development',
  devServer:{
    // host: '192.168.1.100',
   
  }
}